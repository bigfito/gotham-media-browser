# Real Meta ImageBind runtime deps — installed only when INSTALL_IMAGEBIND=true
# (the production default). CPU-only PyTorch wheels keep the image runnable on the
# M4 lab without a GPU. Pinned for reproducibility; the CPU index serves the +cpu
# builds referenced here.
#
# NOTE: `imagebind` itself is installed separately in the Dockerfile with
# --no-deps, because its setup.py pins these same packages (and a specific
# pytorchvideo commit). We mirror that exact pytorchvideo commit below so the two
# never disagree; pinning any other commit yields a ResolutionImpossible clash.
--extra-index-url https://download.pytorch.org/whl/cpu
# numpy must stay <2: torch 2.2.2 wheels are compiled against the NumPy 1.x ABI.
numpy==1.26.4
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2
# timm >=0.9 exposes `timm.layers`, the import path current ImageBind uses.
timm==0.9.16
ftfy==6.1.3
regex==2024.5.15
einops==0.7.0
fvcore==0.1.5.post20221221
iopath==0.1.10
Pillow==10.4.0
soundfile==0.12.1
pytorchvideo @ git+https://github.com/facebookresearch/pytorchvideo.git@6cdc929315aab1b5674b6dcf73b16ec99147735f
